    {
        "name": "Riders",
        "type": "Logistics",
        "quantity": 20,
        "unit_cost": 149.99
    }

{
  "username": "tester",
  "email": "test@mail.com",
  "password": "test@mail.com"
}

{
  "email": "test@mail.com",
  "password": "test@mail.com"
}

### VERBUMDEI MANAGEMENT SERVICE

        --Using waitress to runserver
        waitress-serve --listen=*:8000 server.server.wsgi:application


to add inventory 
apt-get update && apt-get install -y nginx && gunicorn server.wsgi:application --bind 0.0.0.0:8000 --workers 3 & nginx -g 'daemon off;'